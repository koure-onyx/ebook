# FIELD MISMATCH REPORT

Generated by comparing DEEPSEEK_SCHEMA.md with DB_CURRENT_STATE.txt

---

## TOPICS COLLECTION ANALYSIS

### Fields Present in DB (studyvault database):
- _id
- title
- slug
- chapter_id ✓ (matches DeepSeek)
- chapter_number
- topic_number ✓ (matches DeepSeek)
- difficulty ✓ (matches DeepSeek)
- grade_level
- edition_year ✓ (matches DeepSeek)
- clean_html ✓ (matches DeepSeek)
- raw_text ✓ (matches DeepSeek)
- program_id
- book_id ✓ (matches DeepSeek)
- display_order ✓ (matches DeepSeek)
- estimated_read_time ✓ (matches DeepSeek)
- exam_frequency_count
- content ⚠️ (DeepSeek uses content_blocks, not content)
- is_live ⚠️ (DeepSeek uses workflow_status)
- createdAt
- updatedAt
- workflow_status ✓ (matches DeepSeek)

### CRITICAL MISMATCHES:

1. **MISMATCH: topics.content should be topics.content_blocks**
   - DB has: `content` field
   - DeepSeek requires: `content_blocks` array
   - Need to verify if this is actually an array of blocks or just text

2. **MISMATCH: topics.is_live vs topics.workflow_status**
   - DB has BOTH `is_live` (boolean) AND `workflow_status` (string)
   - DeepSeek requires: `workflow_status: 'live'`
   - Should use `workflow_status` and deprecate `is_live`

3. **MISSING: content_blocks array**
   - Need to check if `content` field contains the blocks array or if it's missing entirely
   - DeepSeek requires full content_blocks array with 16 block types

4. **MISSING: formulas array**
   - Not present in DB fields list
   - DeepSeek requires: `formulas` array

5. **MISSING: key_terms array**
   - Not present in DB fields list
   - DeepSeek requires: `key_terms` array

6. **MISSING: book_mcqs array**
   - Not present in DB fields list
   - DeepSeek requires: `book_mcqs` array

7. **MISSING: book_short_questions array**
   - Not present in DB fields list
   - DeepSeek requires: `book_short_questions` array

8. **MISSING: book_problems array**
   - Not present in DB fields list
   - DeepSeek requires: `book_problems` array

9. **MISSING: keywords array**
   - Not present in DB fields list
   - DeepSeek requires: `keywords` array

10. **MISSING: quran_reference object**
    - Not present in DB fields list
    - DeepSeek requires: `quran_reference` object

11. **MISSING: quran_word_alignments array**
    - Not present in DB fields list
    - DeepSeek requires: `quran_word_alignments` array

12. **MISSING: quran_textbook_translation string**
    - Not present in DB fields list
    - DeepSeek requires: `quran_textbook_translation` string

13. **MISSING: quran_textbook_tafsir string**
    - Not present in DB fields list
    - DeepSeek requires: `quran_textbook_tafsir` string

14. **MISSING: seo object**
    - Not present in DB fields list
    - DeepSeek requires: `seo` object with meta_title, meta_description, keywords, source_page

15. **MISSING: title_urdu string**
    - Not present in DB fields list
    - DeepSeek requires: `title_urdu` string

---

## CHAPTERS COLLECTION ANALYSIS

### Fields Present in DB (studyvault database):
- _id
- title
- slug
- chapter_number ✓ (matches DeepSeek)
- book_id ✓ (matches DeepSeek)
- program_id
- board_id ✓ (matches DeepSeek)
- is_live ⚠️ (DeepSeek doesn't specify chapter-level status)
- total_topics
- createdAt
- updatedAt

### MISSING FIELDS (from DeepSeek schema):
- chapter_number_display
- page_start
- page_end
- student_learning_outcomes
- chapter_summary
- seo object

---

## BOOKS COLLECTION ANALYSIS

### Fields Present in DB (studyvault database):
- _id
- title ✓ (matches DeepSeek)
- subject ✓ (matches DeepSeek)
- subject_slug ✓ (matches DeepSeek)
- slug ✓ (matches DeepSeek)
- grade ⚠️ (DeepSeek uses grade_level)
- edition_year ✓ (matches DeepSeek)
- is_current_edition
- is_live ⚠️ (DeepSeek uses workflow_status concept)
- program_id ✓ (matches DeepSeek)
- board_id ✓ (matches DeepSeek)
- total_topics ⚠️ (DeepSeek tracks total_chapters at book level)
- createdAt
- updatedAt
- is_public

### MISMATCHES:

1. **MISMATCH: books.grade should be books.grade_level**
   - DB has: `grade`
   - DeepSeek requires: `grade_level`

2. **MISSING: publisher string**
   - Not present in DB fields list
   - DeepSeek requires: `publisher`

3. **MISSING: authors array**
   - Not present in DB fields list
   - DeepSeek requires: `authors` array

4. **MISSING: board string**
   - Not present in DB fields list (DB has board_id reference instead)
   - DeepSeek requires: `board` (full name string)

5. **MISSING: language string**
   - Not present in DB fields list
   - DeepSeek requires: `language`

6. **MISSING: script_direction string**
   - Not present in DB fields list
   - DeepSeek requires: `script_direction`

---

## STATUS FIELD SUMMARY

The DB currently has BOTH:
- `is_live` (boolean)
- `workflow_status` (string)

DeepSeek contract specifies using `workflow_status` with value `'live'`.

RECOMMENDATION: Use `workflow_status` as the primary status field.
Values should be: 'draft', 'live', 'archived'

---

## CONTENT BLOCKS VERIFICATION NEEDED

Need to run additional query to check what's inside the `content` field:
- Is it a string or an array?
- If array, does it have the 16 block types from DeepSeek?
- Does it have block_order, type, html fields?

---

END OF MISMATCH REPORT

---

## UPDATED ANALYSIS AFTER READING MODEL FILES

### TOPIC MODEL (Topic.js) - STATUS: MOSTLY CORRECT ✓

The Topic.js model already has most DeepSeek fields:
- content_blocks: [ContentBlockSchema] ✓
- raw_text: String ✓
- clean_html: String ✓
- quran_reference: Object ✓
- quran_word_alignments: Array ✓
- quran_textbook_translation: String ✓
- quran_textbook_tafsir: String ✓
- formulas: Array ✓
- key_terms: Array ✓
- book_mcqs: Array ✓
- book_short_questions: Array ⚠️ (model has [String], DeepSeek wants objects)
- book_problems: Array ✓
- keywords: [String] ✓
- seo: Object ✓
- title_urdu: String ✓
- workflow_status: String ✓
- is_live: Boolean ✓ (also present, can coexist)

**ISSUES FOUND:**
1. book_short_questions should be array of objects with {question, answer, marks}, not just [String]
2. The DB has `content` field (string) but model expects `content_blocks` (array) - ingestion issue
3. Missing fields in DB that exist in model: needs data migration

### BOOK MODEL (Book.js) - STATUS: NEEDS FIXES ⚠️

Current Book.js fields vs DeepSeek:
- Has `grade` (DeepSeek wants `grade_level` at top level)
- Has metadata.grade_level (nested, not top-level)
- Has board (string) ✓
- Missing: authors at top level (has metadata.authors)
- Missing: publisher at top level (has metadata.publisher)
- Missing: language at top level (has metadata.language)
- Missing: script_direction at top level (has metadata.script_direction)

**RECOMMENDATION:** Add top-level grade_level field for DeepSeek compatibility

### CHAPTER MODEL (Chapter.js) - STATUS: MOSTLY CORRECT ✓

Chapter.js has:
- chapter_number ✓
- chapter_number_display ✓
- book_id ✓
- student_learning_outcomes ✓
- summary (DeepSeek calls it chapter_summary) ⚠️
- page_start ✓
- page_end ✓
- seo ✓
- is_live ✓

**MINOR ISSUE:** summary should be renamed chapter_summary for exact DeepSeek match

---

## ROOT CAUSE ANALYSIS

The DB documents are MISSING content_blocks because:
1. The ingestion pipeline is saving to `content` field instead of `content_blocks`
2. Need to check ingestion.service.js

The DB documents are MISSING all the Quran/formulas/MCQ arrays because:
1. These fields exist in the Mongoose model but aren't being populated during ingestion
2. Need to fix the ingestion service to save ALL DeepSeek fields

---

END OF UPDATED REPORT
